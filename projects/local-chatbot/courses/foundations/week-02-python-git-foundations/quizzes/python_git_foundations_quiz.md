# Python + Git Foundations — Quiz

Answer from memory before re-reading the lesson notes.

## Questions

1. What does `if __name__ == "__main__":` prevent when you import a module?

2. Why use `pathlib.Path` instead of string paths like `"folder/file.json"`?

3. What is the difference between `git add` and `git commit`?

4. Why do AI teams store experiment configs in JSON or YAML files under version control?

5. What command shows your last three commits in one line each?

## Reflection

Describe a real AI workflow (fine-tuning, prompt iteration, or evaluation) where Git history would help you understand *when* a regression was introduced.

**Your answer:**

_(Write here.)_
