# Python + Git Foundations — Review Questions

Answer aloud or in one sentence each. No notes.

## Warm-Up

1. What file format does the lab config manager read and write?

2. What Git command shows which files changed since the last commit?

## Core Recall

3. Explain `if __name__ == "__main__":` in plain language.

4. Why use UTC ISO timestamps for `last_run` instead of a local time string?

5. What belongs in a good commit message for an AI engineering repo?

## Application

6. Your teammate changes a prompt template and model accuracy drops. Which Git command helps you find when that file last changed, and why does that matter?

## Self-Check

- [ ] I ran `python_git_foundations.py` and saw lab_config.json update.
- [ ] I completed the Git checkpoint with a descriptive commit message.
- [ ] I can explain why configs and code belong in the same repo.
