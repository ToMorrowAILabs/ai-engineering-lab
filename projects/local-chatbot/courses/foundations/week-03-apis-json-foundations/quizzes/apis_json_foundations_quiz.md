# APIs + JSON Foundations — Quiz

Answer from memory before re-reading the lesson notes.

## Questions

1. What HTTP method does the exercise use, and when would you use POST instead?

2. What does `response.raise_for_status()` do?

3. Name two HTTP status codes that indicate failure and what each means.

4. Why write API results to a JSON file instead of only printing them?

5. LLMs sometimes wrap JSON in markdown code fences. How would you clean that before `json.loads()`?

## Reflection

Sketch the JSON shape you expect from an Ollama `/api/generate` response (fields like `response`, `model`). Which fields would you log for debugging a slow or malformed reply?

**Your answer:**

_(Write here.)_
