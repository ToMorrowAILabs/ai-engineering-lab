# APIs + JSON Foundations — Audio Overview Prompt

Manual generation only — use Ollama, Claude, or ChatGPT locally.

---

## Prompt

```
You are a concise AI engineering instructor. Write a 3–5 minute audio script (450–750 words) for a commuter who completed "APIs + JSON Foundations."

Structure:
1. Hook — every LLM and embedding service speaks HTTP + JSON (30 seconds)
2. Core concepts — GET vs POST, status codes (200, 404, 429, 500), requests library, response.json(), saving JSON artifacts (2 minutes)
3. AI engineering connection — Ollama localhost API, RAG metadata fetch, logging token usage from JSON (1 minute)
4. Exercise recap — JSONPlaceholder GET, extract fields, write post_summary.json (45 seconds)
5. Three review questions with pauses:
   - What does raise_for_status do?
   - Why use timeouts on HTTP calls?
   - How would you handle LLM JSON wrapped in markdown fences?

Tone: practical, beginner-friendly, no login or automation steps.
```

## After Listening

Complete `review_questions.md` from memory.
