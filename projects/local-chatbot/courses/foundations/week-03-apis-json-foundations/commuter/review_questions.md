# APIs + JSON Foundations — Review Questions

## Warm-Up

1. What HTTP method does the exercise use?

2. What status code means "success" for a GET request?

## Core Recall

3. Why call `raise_for_status()` before parsing JSON?

4. Name three fields extracted from the JSONPlaceholder post.

5. Why set `timeout=10` on `requests.get()`?

## Application

6. An Ollama call returns HTTP 200 but the `response` field contains JSON wrapped in ```json fences. List the steps to get a Python dict safely.

## Self-Check

- [ ] I ran the exercise and created `post_summary.json`.
- [ ] I can explain GET vs POST in one sentence each.
- [ ] I completed the Git checkpoint.
